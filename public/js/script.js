/*
 * Au clic sur un bouton, celui-ci devient sélectionné.
 * On doit donc changer l'état du nouveau bouton mais aussi du bouton actuel
 * car un seul bouton peut-être sélectionné à la fois.
 * Parfois, il n'y a pas 2 mais 3 boutons.
 */

$(function() {
	/*
	$('#door-close').addEventListener('click', changeEtatDoor, false);
	$('#door-open').addeventlistener('click', changeEtatDoor, false);
// quand je clique sur CE bouton, je supprime les classes de TOUS CES boutons et je remets la classe sur CE bouton
	function changeEtatDoor() {
	//	this.disabled = disabled;
		this.addClass(' selection');
	}


	$('#pane-open').addEventListener('click', changeEtatPane, false);
	$('#pane-close').addeventlistener('click', changeEtatPane, false);
	$('#pane-half').addeventlistener('click', changeEtatPane, false);

	function changeEtatPane() {
	//	this.disabled = disabled;
		this.className = this.className + ' selection';
	}


	$('#heat-on').addEventListener('click', changeEtatHeat, false);
	$('#heat-off').addeventlistener('click', changeEtatHeat, false);

	function changeEtatHeat() {
	//	this.disabled = disabled;
		this.className = this.className + ' selection';
	}
	
	$('#phone-modif').addEventListener('click', modifNumber, false);
	function modifNumber() {
		$('#phone-number').disabled = 'false';
	}
*/
});